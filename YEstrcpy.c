#include "shell.h"

/**
 * YE_strcpy - copies the string input
 * @field: string destination input
 * @tank: string source input
 * Return: copied string
 */

void *YE_strcpy(char *field, char *tank)
{
int a;
int b = 0;

for (a = 0; tank[a] != '\0'; a++)
{
}
while (b <= a)
{
field[b] = tank[b];
b++;
}
return (field);
}
